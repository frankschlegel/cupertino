# search_samples (Legacy)

Legacy documentation page for the old `search_samples` name.

Use the unified `search` MCP tool with `source: "samples"` (or `source: "apple-sample-code"`).

## Use Instead

```json
{
  "name": "search",
  "arguments": {
    "query": "SwiftUI animation",
    "source": "samples",
    "limit": 10
  }
}
```

## See Also

- [search](../search/) - Canonical unified search tool docs
- [list_samples](../list_samples/) - List indexed samples
- [read_sample](../read_sample/) - Read sample README and metadata
