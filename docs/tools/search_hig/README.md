# search_hig (Legacy)

Legacy documentation page for the old `search_hig` name.

Use the unified `search` MCP tool with `source: "hig"`.

## Use Instead

```json
{
  "name": "search",
  "arguments": {
    "query": "buttons",
    "source": "hig",
    "limit": 10
  }
}
```

## See Also

- [search](../search/) - Canonical unified search tool docs
- [read_document](../read_document/) - Read guideline documents by URI
